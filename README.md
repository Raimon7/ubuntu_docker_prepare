A simple script to prepare a fresh Ubuntu machine as a docker host.

1. clone or download the script to your host
2. run:  chmod +x prepare.sh
3. run (if root):      ./prepare.sh 
    or (if not root):  sudo ./prepare.sh
    
